paths:
  preferred_dataset: data/qlib/qlib_market_dataset.parquet
  fallback_dataset: data/features/market_features_60d.parquet
  output_report: data/reports/phase21_qlib_walkforward_report.json
  output_dir: data/reports/phase21_walkforward

runtime:
  max_rows: 50000
  folds: 5
  min_train_rows: 2000
  min_test_rows: 500
  random_state: 42
  max_features: 40

target:
  preferred_column: target_direction_3
  fallback_return_column: future_ret_3

simulation:
  return_column: future_ret_3
  fee_bps_roundtrip: 8.0

model:
  preferred_backend: lightgbm
  fallback_backend: sklearn_hist_gradient_boosting
